<Window x:Class="AttachedBehavior.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AttachedBehavior"
        Title="MainWindow" Height="350" Width="525">
    <StackPanel>
        <Button local:KikooEffects.IsBlinking="{Binding IsChecked,ElementName=choiceInput}">Hello</Button>
        <CheckBox x:Name="choiceInput">Activate killer effect!</CheckBox>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"
            </Grid.RowDefinitions>
        </Grid>
    </StackPanel>
</Window>
